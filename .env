# Compose CLI only (read by `docker compose`, not merged into container env).
# Limits concurrent engine work so parallel image builds do not interleave logs.
# Application settings belong in docker-compose.yaml under each service's environment:.
COMPOSE_PARALLEL_LIMIT=1
